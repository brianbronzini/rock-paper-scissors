Randomize and return the computer's selection:
  array [ROCK, PAPER, SCISSORS]
  randomizer
  variable for result
  return result

function game() {
  for (increase each round by 1 'best of 5')
    get user choice and convert case
    get computer choice
    call playRound(param1, param2)

  function playRound() {
    Declare win conditions:
      if (player === computer)
        then tie (no points awarded)
      else if (
        player-rock && computer-scissors ||
        player-paper && computer-rock ||
        player-scissors && computer-paper)
        )
        player++
      else
        computer++
  }
}

call game();